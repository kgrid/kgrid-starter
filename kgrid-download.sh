#!/usr/bin/env bash

# Download jdk8, virtualbox, vagrant, node.js (plus npm). Then `npm install http-server`

java -version
vboxmanage -version
vagrant --version
node --version

# tell the user that they need to install jdk8, virtualbox, vagrant, node.js

# curl  [kgrid library release] --output ./library
